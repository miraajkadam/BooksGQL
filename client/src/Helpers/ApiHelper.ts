export default class ApiHelper {
  apiUrl: string;

  public constructor() {
    this.apiUrl = 'http://localhost:5000/graphql';
  }

  // public getGraphQLData = async (resource: any, params: any, fields: any) => {
  //   const request = 
  // };
}
